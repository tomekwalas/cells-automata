import { dislocationGun } from "../Recrystalization";

describe("Recrystalization tests", () => {
  test("should get dislocation gun", () => {
    const mesh = [[0, 0, 0][(0, 0, 0)][(0, 0, 0)]];
    const actual = dislocationGun(3, 3, mesh);
  });
});
